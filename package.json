{
  "name": "kol-diet-planner-project",
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "packageManager": "yarn@4.9.1",
  "scripts": {
    "format": "yarn workspaces foreach --all run format"
  }
}
